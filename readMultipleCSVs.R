getmonitor <- function(id, directory, summarize = FALSE) {
ID<-id
directory<-read.csv("STRING/ID().csv")
summarize<-if(summarize==FALSE)
print(getmonitor)
else 
print(summary(getmonitor))}
"%s/%03s.csv",directory,id
03d

#sum<-paste(as.numeric(summarize))
if summarize==TRUE
{print summary(summary(data))}
return(data) 
}


id<-as.integer(id)
filename<-sprintf("%s/%03d.csv",directory,id)
data<-read.csv(filename)
return(data)
